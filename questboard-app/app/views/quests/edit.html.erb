
<% if false %>
<%= form_for :quest, :url => update_quest_path(params[:id]) do |f| %>
<%= f.text_field :title, :value => @quest.title, placeholder: "Enter quest title"%>
<%= f.text_area :description, :value => @quest.description, placeholder: "Enter post content" %>
<%= f.text_field :bounty, :value => @quest.bounty, placeholder: "Enter quest bounty"%>
<%= f.date_select :due_date, :order => [:day, :month, :year] %>
<%= f.check_box :is_completed %>
<%= f.submit "Save", class: "login-button", style: "margin-top: 0.5em;" %>
<% end %>
<% end %>

<header class="header-title">
	Edit Quest<font class="label" style="font-size: 28px"><%= " | #{@quest.title}" %></font>
	<header>
<%# if false %>
<div>
	<%= form_for :quest, :url => update_quest_path(params[:id]) do |f| %>
		<table style="width:80%;border-collapse: separate;border-spacing: 0em 1em;">
			<tr style="padding-bottom: 1em">
				<td><font class="label" style="width:20%;">Title: </font></td>
				<td style="width:80%;"><%= f.text_field :title, placeholder: @quest.title, class: "input", style: "width:100%" %></td>
			</tr>
			<tr style="padding-bottom: 1em">
				<td><font class="label">Description: </font></td>
				<td><%= f.text_area :description, placeholder: @quest.description, class: "textarea", style: "width:100%;resize: vertical;" %></td>
			</tr>
			
			<tr style="padding-bottom: 1em">	
			<td><font class="label">Bounty: </font></td>	
			<td style="width:80%;"><%= f.text_field :bounty,placeholder: @quest.bounty, class: "input", style: "width:100%" %></td>
			</tr>
			<tr style="padding-bottom: 1em;">
				<td><font class="label">Due date: </font></td>
				<td><%= f.date_select :due_date, :order => [:day, :month, :year], value: @quest.due_date %></td>
			</tr>
			<%= f.file_field :image %>	
			<tr><td></td><td colspan="1"><%= f.submit "Save", class: "login-button", class: "button", style: "float: right" %></td></tr>
		</table>
	<% end %>
</div>
<%# end %>
<!-- =======
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
<div class="input-group">
<div class="col-lg-5 col-md-offset-5">
>>>>>>> dc9647488c43836556b212203cece36875a99214

				<tr style="padding-bottom: 1em;">
					<td><font class="label">Due date: </font></td>
					<td><%= f.date_select :due_date, :order => [:day, :month, :year], value: @quest.due_date %></td>
				</tr>
				<tr>
					<td></td><td colspan="1"><%= f.submit "Save", class: "login-button", class: "button", style: "float: right" %></td>
				</tr>
			</table>
			<% end %>
		</div>
		
		
